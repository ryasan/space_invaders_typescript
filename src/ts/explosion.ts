import { getGame } from './app';

// class Particle {
//     x = 200;
//     y = 200;

//     draw = () => {
//         // draw particle
//     };
// }

export default class Explosion {
    coordinates = { x: 200, y: 200 };
    game = getGame();

    update = () => {
        // update
    };

    draw = () => {
        // draw
    };
}
